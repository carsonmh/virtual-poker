import styled from "styled-components";

const LeaderboardSlotWrapper = styled.div`
  display: flex;
  justify-content: space-between;
  background: rgba(0, 0, 0, 0.5);
  padding: 10px;
  border-radius: 3px;
  color: white;
`;
export default LeaderboardSlotWrapper;
